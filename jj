def setup():
    background("#000000")
    size(600,600)
    textSize(100)
def draw():
    pass
def keyPressed():
    if key == "z":
        text(key,mouseX,mouseY)
    if key == "w":
        text(key,mouseX,mouseY)
    if key == "u":
        text(key,mouseX,mouseY)
    if key == "[":
        text(key,mouseX,mouseY)
